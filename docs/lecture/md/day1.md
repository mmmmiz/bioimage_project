# Day 1: NumPy基礎（2〜3時間）
## Bioimage Quality Check App 事前学習

---

## 🎯 今日のゴール

今日の学習を終えると、以下ができるようになります：

- [ ] NumPy配列が「画像データそのもの」であることを理解する
- [ ] `shape`、`dtype`で画像の情報を読み取れる
- [ ] `np.mean()`で明るさを計算できる
- [ ] `np.std()`でコントラストを計算できる
- [ ] `np.var()`でシャープネス計算の準備ができる

---

## 📚 目次

1. [NumPyとは何か](#1-numpyとは何か)
2. [NumPy配列の基本](#2-numpy配列の基本)
3. [画像 = NumPy配列](#3-画像--numpy配列)
4. [統計関数（mean, std, var）](#4-統計関数mean-std-var)
5. [実践演習](#5-実践演習)
6. [確認問題](#6-確認問題)
7. [まとめと次回予告](#7-まとめと次回予告)

---

## 1. NumPyとは何か

### 1.1 ざっくり説明

NumPy（ナムパイ）は、Pythonで「数値の塊」を高速に扱うためのライブラリです。

**たとえ話**：
- Pythonのリスト = 手作業で1つずつ計算する
- NumPy配列 = 電卓で一気にまとめて計算する

### 1.2 なぜ画像処理でNumPyが必要？

画像は「ピクセル（点）の集まり」です。

```
1枚の画像 = 数十万〜数百万のピクセル
各ピクセル = 0〜255の数値（明るさ）
```

この大量の数値を効率よく扱うために、NumPyが必須なのです。

### 1.3 インストール確認

```python
import numpy as np

print(np.__version__)
# 出力例: 1.24.3
```

**💡 ポイント**: `import numpy as np`と書くのが慣例です。以降`np.〇〇`と書けます。

---

## 2. NumPy配列の基本

### 2.1 配列を作ってみる

#### リストから配列を作る

```python
import numpy as np

# 1次元配列（数字が横に並んだもの）
arr1d = np.array([1, 2, 3, 4, 5])
print(arr1d)
# 出力: [1 2 3 4 5]

# 2次元配列（数字が縦横に並んだもの＝表のようなもの）
arr2d = np.array([
    [1, 2, 3],
    [4, 5, 6]
])
print(arr2d)
# 出力:
# [[1 2 3]
#  [4 5 6]]
```

**📝 用語解説**：
- **1次元配列**: 数字が一列に並んだもの。ベクトルとも呼ばれる
- **2次元配列**: 数字が縦横に並んだもの。行列（matrix）とも呼ばれる
- **ndarray**: NumPy配列の正式名称（n-dimensional array = n次元配列）

### 2.2 配列の属性を確認する（超重要！）

配列には「自分がどんな形をしているか」を教えてくれる属性があります。

```python
import numpy as np

arr = np.array([
    [1, 2, 3],
    [4, 5, 6]
])

# shape: 配列の形状（各次元のサイズ）
print(arr.shape)  # (2, 3) ← 2行3列という意味

# dtype: データ型（どんな種類の数値か）
print(arr.dtype)  # int64 ← 64ビット整数という意味

# ndim: 次元数
print(arr.ndim)   # 2 ← 2次元という意味

# size: 全要素数
print(arr.size)   # 6 ← 2×3=6個の要素があるという意味
```

#### 属性の意味まとめ

| 属性 | 意味 | 例 |
|------|------|-----|
| `shape` | 形状（タプル） | `(2, 3)` = 2行3列 |
| `dtype` | データ型 | `int64`, `float64`, `uint8` |
| `ndim` | 次元数 | `1`, `2`, `3` |
| `size` | 全要素数 | `6` |

### 2.3 dtype（データ型）の種類

プロジェクトでよく使うデータ型：

| dtype | 意味 | 範囲 | 用途 |
|-------|------|------|------|
| `uint8` | 符号なし8ビット整数 | 0〜255 | 画像のピクセル値 |
| `int64` | 符号あり64ビット整数 | 非常に広い | 一般的な整数 |
| `float64` | 64ビット浮動小数点 | 非常に広い | 計算結果 |

**📝 用語解説**：
- **uint8**: unsigned int 8-bit の略。「符号なし（マイナスなし）8ビット整数」
- 画像は通常`uint8`で表現される（0=黒、255=白）

```python
import numpy as np

# データ型を指定して配列を作る
arr_uint8 = np.array([0, 128, 255], dtype=np.uint8)
print(arr_uint8.dtype)  # uint8
```

### 📍 ここがわからなければ確認すべき基礎

- Pythonのリスト（`[]`での作成、要素へのアクセス）
- タプル（`()`で表現される変更不可のデータ構造）
- 変数への代入とprint文

---

## 3. 画像 = NumPy配列

### 3.1 画像はどうやって数値になる？

画像は「ピクセルの集まり」で、各ピクセルは数値で表されます。

#### グレースケール画像（白黒画像）

```
┌─────────────────────────┐
│ 0   50  100 150 200 255 │  ← 各数値がピクセルの明るさ
│ 黒  暗   ↓   ↓   明  白 │
└─────────────────────────┘
```

- **0** = 真っ黒
- **255** = 真っ白
- **中間の値** = グレー（灰色）

#### グレースケール画像の配列

```python
import numpy as np

# 4×4ピクセルのグレースケール画像を想像してください
gray_image = np.array([
    [0,   50,  100, 150],   # 1行目：暗い → 明るい
    [50,  100, 150, 200],   # 2行目
    [100, 150, 200, 250],   # 3行目
    [150, 200, 250, 255]    # 4行目：最も明るい
], dtype=np.uint8)

print(gray_image.shape)  # (4, 4) ← 高さ4、幅4
print(gray_image.ndim)   # 2 ← 2次元
```

### 3.2 カラー画像の配列

カラー画像は**3つのチャンネル**（層）を持ちます。

```
┌──────────────────────────────────────┐
│  赤(R)チャンネル   [0〜255の2次元配列]  │
│  緑(G)チャンネル   [0〜255の2次元配列]  │
│  青(B)チャンネル   [0〜255の2次元配列]  │
└──────────────────────────────────────┘
         ↓ 3つを重ねると
    カラー画像（3次元配列）
```

```python
import numpy as np

# 2×2ピクセルのカラー画像を想像してください
# shape = (高さ, 幅, チャンネル数)
color_image = np.zeros((2, 2, 3), dtype=np.uint8)

# 左上のピクセルを赤色にする
color_image[0, 0] = [255, 0, 0]  # R=255, G=0, B=0 → 赤

# 右上のピクセルを緑色にする
color_image[0, 1] = [0, 255, 0]  # R=0, G=255, B=0 → 緑

print(color_image.shape)  # (2, 2, 3) ← 高さ2、幅2、3チャンネル
print(color_image.ndim)   # 3 ← 3次元
```

### 3.3 shapeの読み方（超重要！）

| 画像の種類 | shape | 意味 |
|-----------|-------|------|
| グレースケール | `(480, 640)` | 高さ480px、幅640px |
| カラー(RGB) | `(480, 640, 3)` | 高さ480px、幅640px、3チャンネル |

**覚え方**: `(高さ, 幅, チャンネル)` = `(Height, Width, Channels)` = `(H, W, C)`

```python
import numpy as np

# グレースケール画像のshape
gray = np.zeros((480, 640), dtype=np.uint8)
print(f"グレースケール: {gray.shape}")  # (480, 640)
print(f"次元数: {gray.ndim}")           # 2

# カラー画像のshape
color = np.zeros((480, 640, 3), dtype=np.uint8)
print(f"カラー: {color.shape}")         # (480, 640, 3)
print(f"次元数: {color.ndim}")          # 3
```

### 3.4 カラーかグレースケールかを判定する

```python
def is_grayscale(image):
    """画像がグレースケールかどうかを判定"""
    return image.ndim == 2

# テスト
gray = np.zeros((100, 100), dtype=np.uint8)
color = np.zeros((100, 100, 3), dtype=np.uint8)

print(is_grayscale(gray))   # True
print(is_grayscale(color))  # False
```

### 📍 ここがわからなければ確認すべき基礎

- Pythonのインデックス（`配列[0]`で最初の要素にアクセス）
- スライス（`配列[0:3]`で範囲を指定）
- 2次元配列へのアクセス（`配列[行][列]`または`配列[行, 列]`）

---

## 4. 統計関数（mean, std, var）

ここからがプロジェクトの核心部分です。  
**3つの統計関数で画像の品質指標を計算**します。

### 4.1 np.mean()：平均（明るさ計算）

#### ざっくり説明

「全部足して、個数で割る」＝ 平均

#### 数式

$$
\text{平均} = \frac{\text{全要素の合計}}{\text{要素数}}
$$

#### コード例

```python
import numpy as np

# 簡単な例
arr = np.array([10, 20, 30, 40, 50])
mean_value = np.mean(arr)
print(mean_value)  # 30.0  ← (10+20+30+40+50)/5 = 150/5 = 30

# 2次元配列（画像）の場合
image = np.array([
    [100, 150],
    [200, 250]
])
mean_brightness = np.mean(image)
print(mean_brightness)  # 175.0  ← (100+150+200+250)/4 = 700/4 = 175
```

#### 画像での意味

```
平均輝度 = 画像全体の「明るさ」

低い値（0に近い） → 暗い画像
高い値（255に近い） → 明るい画像
```

#### 明るさ計算関数（プロジェクトで実装するもの）

```python
import numpy as np

def calc_brightness(image: np.ndarray) -> float:
    """
    画像の平均輝度（明るさ）を計算する
    
    Parameters
    ----------
    image : np.ndarray
        入力画像（グレースケールを想定）
    
    Returns
    -------
    float
        平均輝度値（0.0〜255.0）
    
    Examples
    --------
    >>> img = np.array([[100, 150], [200, 250]], dtype=np.uint8)
    >>> calc_brightness(img)
    175.0
    """
    return float(np.mean(image))


# 使用例
test_image = np.array([
    [50, 100, 150],
    [100, 150, 200],
    [150, 200, 250]
], dtype=np.uint8)

brightness = calc_brightness(test_image)
print(f"明るさ: {brightness:.1f}")  # 明るさ: 150.0
```

### 4.2 np.std()：標準偏差（コントラスト計算）

#### ざっくり説明

「値がどれくらいバラついているか」を表す数値

- バラつきが小さい → 標準偏差が小さい
- バラつきが大きい → 標準偏差が大きい

#### 直感的な理解

```python
import numpy as np

# バラつきなし（全部同じ値）
arr_same = np.array([50, 50, 50, 50])
print(np.std(arr_same))  # 0.0 ← 全く散らばっていない

# バラつきあり
arr_spread = np.array([0, 50, 50, 100])
print(np.std(arr_spread))  # 35.36... ← 散らばっている

# もっとバラつきあり
arr_wide = np.array([0, 0, 100, 100])
print(np.std(arr_wide))  # 50.0 ← さらに散らばっている
```

#### 数式（参考）

$$
\text{標準偏差} = \sqrt{\frac{1}{N}\sum_{i=1}^{N}(x_i - \bar{x})^2}
$$

**日本語で言うと**：「各値と平均の差を2乗して、平均して、ルートを取る」

#### 画像での意味

```
標準偏差 = 画像の「コントラスト」

低い値 → コントラストが低い（のっぺりした画像）
高い値 → コントラストが高い（メリハリのある画像）
```

#### コントラスト計算関数

```python
import numpy as np

def calc_contrast(image: np.ndarray) -> float:
    """
    画像のコントラスト（標準偏差）を計算する
    
    Parameters
    ----------
    image : np.ndarray
        入力画像（グレースケールを想定）
    
    Returns
    -------
    float
        コントラスト値（0.0〜127.5程度が一般的）
    
    Notes
    -----
    - 値が0に近い：コントラストが低い（のっぺり）
    - 値が大きい：コントラストが高い（メリハリがある）
    """
    return float(np.std(image))


# 使用例
# 低コントラスト画像（似たような値ばかり）
low_contrast = np.array([
    [120, 125, 130],
    [125, 130, 135],
    [130, 135, 140]
], dtype=np.uint8)

# 高コントラスト画像（極端に違う値が混在）
high_contrast = np.array([
    [0,   255, 0],
    [255, 0,   255],
    [0,   255, 0]
], dtype=np.uint8)

print(f"低コントラスト: {calc_contrast(low_contrast):.1f}")   # 約6.2
print(f"高コントラスト: {calc_contrast(high_contrast):.1f}") # 約127.5
```

### 4.3 np.var()：分散（シャープネス計算の準備）

#### ざっくり説明

「標準偏差を2乗したもの」＝ 分散

#### 関係性

$$
\text{分散} = (\text{標準偏差})^2
$$

または

$$
\text{標準偏差} = \sqrt{\text{分散}}
$$

#### コード例

```python
import numpy as np

arr = np.array([0, 50, 50, 100])

variance = np.var(arr)
std_dev = np.std(arr)

print(f"分散: {variance}")           # 1250.0
print(f"標準偏差: {std_dev}")        # 35.36...
print(f"標準偏差の2乗: {std_dev**2}") # 1250.0（分散と同じ）
```

#### なぜ分散を使うのか？

**シャープネス計算では分散を使います**。理由：

1. Laplacianフィルタ（Day 2で学習）の出力にはマイナス値が含まれる
2. 標準偏差より分散の方が計算が簡単
3. 慣例的にシャープネス指標は分散で計算される

#### シャープネス計算の予告（Day 2で実装）

```python
import numpy as np
import cv2  # Day 2で学習

def calc_sharpness(image: np.ndarray) -> float:
    """
    画像のシャープネス（Laplacian分散）を計算する
    
    ※ cv2.Laplacian()はDay 2で学習します
    """
    # グレースケールに変換（必要な場合）
    if image.ndim == 3:
        gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)
    else:
        gray = image
    
    # Laplacianフィルタを適用
    laplacian = cv2.Laplacian(gray, cv2.CV_64F)
    
    # 分散を計算
    return float(np.var(laplacian))
```

### 4.4 3つの関数の比較

| 関数 | 計算内容 | プロジェクトでの用途 | 実装日 |
|------|----------|---------------------|--------|
| `np.mean()` | 平均 | 明るさ（Brightness） | Day 3 |
| `np.std()` | 標準偏差 | コントラスト（Contrast） | Day 4 |
| `np.var()` | 分散 | シャープネス（Sharpness） | Day 5 |

### 📍 ここがわからなければ確認すべき基礎

- 平均の計算方法（足して割る）
- 平方根（ルート）の概念
- 関数の定義と戻り値（`def`、`return`）

---

## 5. 実践演習

実際にコードを書いて確認しましょう。  
VS CodeのNotebook（.ipynb）またはPythonファイルで実行してください。

### 演習1: 配列の属性を確認する

```python
import numpy as np

# 演習: 以下の配列の shape, dtype, ndim を予想してから実行

# 配列A
arr_a = np.array([1, 2, 3, 4, 5])
print("配列A:")
print(f"  shape: {arr_a.shape}")
print(f"  dtype: {arr_a.dtype}")
print(f"  ndim: {arr_a.ndim}")

# 配列B
arr_b = np.array([[1, 2, 3], [4, 5, 6]], dtype=np.uint8)
print("\n配列B:")
print(f"  shape: {arr_b.shape}")
print(f"  dtype: {arr_b.dtype}")
print(f"  ndim: {arr_b.ndim}")

# 配列C（3次元＝カラー画像を想定）
arr_c = np.zeros((100, 200, 3), dtype=np.uint8)
print("\n配列C:")
print(f"  shape: {arr_c.shape}")
print(f"  dtype: {arr_c.dtype}")
print(f"  ndim: {arr_c.ndim}")
```

### 演習2: 統計値を計算する

```python
import numpy as np

# 暗い画像を想定
dark_image = np.array([
    [10, 20, 30],
    [20, 30, 40],
    [30, 40, 50]
], dtype=np.uint8)

# 明るい画像を想定
bright_image = np.array([
    [200, 210, 220],
    [210, 220, 230],
    [220, 230, 240]
], dtype=np.uint8)

print("暗い画像:")
print(f"  平均（明るさ）: {np.mean(dark_image):.1f}")
print(f"  標準偏差（コントラスト）: {np.std(dark_image):.1f}")

print("\n明るい画像:")
print(f"  平均（明るさ）: {np.mean(bright_image):.1f}")
print(f"  標準偏差（コントラスト）: {np.std(bright_image):.1f}")

# 質問: どちらの画像の「明るさ」が高い？
# 質問: コントラストはどうか？
```

### 演習3: 指標計算関数を作る

```python
import numpy as np

def calc_brightness(image: np.ndarray) -> float:
    """画像の明るさを計算"""
    # ここを埋める
    pass

def calc_contrast(image: np.ndarray) -> float:
    """画像のコントラストを計算"""
    # ここを埋める
    pass

# テスト用画像
test_image = np.array([
    [0, 50, 100],
    [50, 100, 150],
    [100, 150, 200]
], dtype=np.uint8)

# テスト実行
print(f"明るさ: {calc_brightness(test_image)}")      # 期待値: 100.0
print(f"コントラスト: {calc_contrast(test_image)}")  # 期待値: 約52.7
```

<details>
<summary>演習3の解答を見る</summary>

```python
import numpy as np

def calc_brightness(image: np.ndarray) -> float:
    """画像の明るさを計算"""
    return float(np.mean(image))

def calc_contrast(image: np.ndarray) -> float:
    """画像のコントラストを計算"""
    return float(np.std(image))

# テスト用画像
test_image = np.array([
    [0, 50, 100],
    [50, 100, 150],
    [100, 150, 200]
], dtype=np.uint8)

# テスト実行
print(f"明るさ: {calc_brightness(test_image)}")      # 100.0
print(f"コントラスト: {calc_contrast(test_image)}")  # 52.70...
```
</details>

---

## 6. 確認問題

### 問題1: shapeの読み取り

以下の配列の`shape`を答えてください（実行せずに）。

```python
import numpy as np

a = np.array([[1, 2], [3, 4], [5, 6]])
# a.shape = ?

b = np.zeros((480, 640, 3))
# b.shape = ?
# これはカラー画像？グレースケール画像？
```

### 問題2: 統計関数の選択

次の用途に適した関数を選んでください。

| 用途 | 選択肢 |
|------|--------|
| 画像全体の明るさを測りたい | A: np.mean / B: np.std / C: np.var |
| 画像のコントラストを測りたい | A: np.mean / B: np.std / C: np.var |
| Laplacianフィルタ後のバラつきを測りたい | A: np.mean / B: np.std / C: np.var |

### 問題3: 直感問題

以下の2つの画像、どちらが：
- 明るい？
- コントラストが高い？

```python
image_a = np.array([[50, 50], [50, 50]], dtype=np.uint8)   # 全部50
image_b = np.array([[0, 100], [100, 200]], dtype=np.uint8) # バラバラ
```

### 問題4: コード修正

以下のコードにはエラーがあります。修正してください。

```python
import numpy as np

def calc_brightness(image):
    return np.mean  # ← 何かがおかしい

test = np.array([[100, 200], [150, 250]])
print(calc_brightness(test))
```

<details>
<summary>確認問題の解答</summary>

**問題1:**
```
a.shape = (3, 2)  ← 3行2列
b.shape = (480, 640, 3)  ← カラー画像（3チャンネルあるから）
```

**問題2:**
- 明るさ → A: np.mean
- コントラスト → B: np.std
- Laplacian後のバラつき → C: np.var

**問題3:**
- 明るい方: image_b（平均100 > image_aの平均50）
- コントラストが高い方: image_b（値がバラバラ）
  - image_a: std=0（全部同じ値）
  - image_b: std≒70.7（大きくバラついている）

**問題4:**
```python
def calc_brightness(image):
    return np.mean(image)  # ← (image) を追加
```
`np.mean`は関数オブジェクト、`np.mean(image)`は関数の呼び出し結果
</details>

---

## 7. まとめと次回予告

### 今日学んだこと

| トピック | 内容 |
|----------|------|
| NumPy配列 | 画像は数値の配列として表現される |
| shape | 配列の形状（高さ, 幅, チャンネル） |
| dtype | データ型（画像はuint8が多い） |
| np.mean() | 平均 → 明るさの計算に使用 |
| np.std() | 標準偏差 → コントラストの計算に使用 |
| np.var() | 分散 → シャープネスの計算に使用 |

### プロジェクトとの対応

| 今日の学習 | プロジェクトでの使用日 |
|-----------|----------------------|
| shape, dtype | Day 1（画像読み込み確認） |
| ndimでカラー/グレー判定 | Day 7（カラー/グレー両対応） |
| np.mean() | Day 3（calc_brightness関数） |
| np.std() | Day 4（calc_contrast関数） |
| np.var() | Day 5（calc_sharpness関数） |

### 次回予告: Day 2 OpenCV基礎

明日は以下を学習します：

- `cv2.imread()`: 画像ファイルの読み込み
- `cv2.cvtColor()`: カラー変換（BGR → グレースケール）
- `cv2.Laplacian()`: シャープネス計算用フィルタ
- `cv2.GaussianBlur()`: 人工ピンぼけ生成

---

## 📚 参考リンク

### 公式ドキュメント
- [NumPy公式ドキュメント（日本語）](https://numpy.org/doc/stable/index.html)
- [np.mean](https://numpy.org/doc/stable/reference/generated/numpy.mean.html)
- [np.std](https://numpy.org/doc/stable/reference/generated/numpy.std.html)
- [np.var](https://numpy.org/doc/stable/reference/generated/numpy.var.html)

### 画像処理の基礎知識
- [画像のデジタル表現（Wikipedia）](https://ja.wikipedia.org/wiki/%E3%83%87%E3%82%B8%E3%82%BF%E3%83%AB%E7%94%BB%E5%83%8F)

---

## 🎯 チェックリスト（学習完了確認）

- [ ] NumPy配列の`shape`から画像サイズを読み取れる
- [ ] `dtype`の意味を理解している（uint8 = 0〜255）
- [ ] `ndim`でカラー/グレースケールを判定できる
- [ ] `np.mean()`で平均（明るさ）を計算できる
- [ ] `np.std()`で標準偏差（コントラスト）を計算できる
- [ ] `np.var()`で分散を計算できる
- [ ] 演習問題をすべて解けた

すべてにチェックが入ったら、Day 2に進みましょう！
