# Bioimage Quality Check App
## 事前学習アジェンダ（推定10〜13時間）

---

## 📋 このアジェンダについて

プロジェクト開始前に必要な知識を「最小限かつ効果的」に学ぶためのカリキュラムです。  
各トピックはプロジェクトで**実際に使う機能だけ**に絞っています。

### 学習の進め方
1. 各セクションを順番に進める
2. 「✅ 確認問題」で理解度をチェック
3. 「🔗 公式リファレンス」で詳細を確認（必要に応じて）
4. 「💡 プロジェクトでの使用場面」で実践イメージを持つ

---

## 🎯 学習ロードマップ

```
Day 1: NumPy基礎（2〜3h）
    ↓
Day 2: OpenCV基礎（2〜3h）
    ↓
Day 3: Python関数・エラー処理（1〜2h）
    ↓
Day 4: Streamlit基礎（3〜4h）
    ↓
Day 5: pandas基礎 + 総復習（1〜2h）
```

---

# Day 1: NumPy基礎（2〜3時間）

## 1.1 NumPy配列とは何か

### 学習目標
- [ ] NumPy配列（ndarray）の概念を理解する
- [ ] 画像データがNumPy配列として扱われることを理解する

### 学ぶべき内容

#### 1.1.1 NumPy配列の作成と基本操作
```python
import numpy as np

# 配列の作成
arr = np.array([1, 2, 3, 4, 5])

# 2次元配列（行列）
matrix = np.array([[1, 2, 3],
                   [4, 5, 6]])
```

#### 1.1.2 配列の属性（プロジェクトで最重要）
| 属性 | 意味 | 画像での例 |
|------|------|------------|
| `shape` | 配列の形状（各次元のサイズ） | `(480, 640, 3)` = 高さ480、幅640、3チャンネル |
| `dtype` | データ型 | `uint8` = 0〜255の整数 |
| `ndim` | 次元数 | 2 = グレースケール、3 = カラー |

```python
import numpy as np

# 画像を読み込んだと仮定
image = np.zeros((480, 640, 3), dtype=np.uint8)

print(image.shape)  # (480, 640, 3)
print(image.dtype)  # uint8
print(image.ndim)   # 3
```

### 💡 プロジェクトでの使用場面
- Day 1: 画像を読み込んだ後、`shape`と`dtype`で画像の情報を確認する
- Day 7: カラー/グレースケールの判定に`ndim`を使う

### 🔗 公式リファレンス
- [NumPy配列の属性](https://numpy.org/doc/stable/reference/arrays.ndarray.html#array-attributes)

---

## 1.2 統計関数（指標計算の核心）

### 学習目標
- [ ] `np.mean()`（平均）の使い方を理解する
- [ ] `np.std()`（標準偏差）の使い方を理解する
- [ ] `np.var()`（分散）の使い方を理解する

### 学ぶべき内容

#### 1.2.1 平均（Mean）→ 明るさ計算に使用
```python
import numpy as np

# 1次元配列の平均
arr = np.array([10, 20, 30, 40, 50])
print(np.mean(arr))  # 30.0

# 2次元配列（画像）の全体平均
image = np.array([[100, 150],
                  [200, 250]])
print(np.mean(image))  # 175.0
```

**平均の意味**: 全ての値を足して、個数で割った値  
**画像での意味**: 全ピクセルの輝度の平均 = 画像全体の「明るさ」

#### 1.2.2 標準偏差（Standard Deviation）→ コントラスト計算に使用
```python
# 標準偏差
arr1 = np.array([50, 50, 50, 50])  # バラつきなし
arr2 = np.array([0, 50, 50, 100])  # バラつきあり

print(np.std(arr1))  # 0.0
print(np.std(arr2))  # 35.36...
```

**標準偏差の意味**: 値が平均からどれくらい散らばっているか  
**画像での意味**: 輝度のバラつき = 画像の「コントラスト」

#### 1.2.3 分散（Variance）→ シャープネス計算に使用
```python
# 分散 = 標準偏差の2乗
arr = np.array([0, 50, 50, 100])
print(np.var(arr))  # 1250.0
print(np.std(arr) ** 2)  # 1250.0（同じ値）
```

**分散の意味**: 標準偏差を2乗したもの。散らばり具合の別表現  
**画像での意味**: Laplacianフィルタ後の分散 = 「シャープネス」

### 💡 プロジェクトでの使用場面
| 関数 | 使用日 | 用途 |
|------|--------|------|
| `np.mean()` | Day 3 | `calc_brightness()` 関数 |
| `np.std()` | Day 4 | `calc_contrast()` 関数 |
| `np.var()` | Day 5 | `calc_sharpness()` 関数 |

### 🔗 公式リファレンス
- [np.mean](https://numpy.org/doc/stable/reference/generated/numpy.mean.html)
- [np.std](https://numpy.org/doc/stable/reference/generated/numpy.std.html)
- [np.var](https://numpy.org/doc/stable/reference/generated/numpy.var.html)

---

## 1.3 ✅ NumPy確認問題

以下のコードの出力を予想してから実行してみてください。

```python
import numpy as np

# 問題1: shape
arr = np.array([[1, 2, 3], [4, 5, 6]])
print(arr.shape)  # → ?

# 問題2: mean
image = np.array([[0, 100], [100, 200]])
print(np.mean(image))  # → ?

# 問題3: std（直感で答える）
low_contrast = np.array([50, 50, 50, 50])
high_contrast = np.array([0, 0, 100, 100])
# どちらの標準偏差が大きい？
```

<details>
<summary>解答を見る</summary>

```
問題1: (2, 3)  # 2行3列
問題2: 100.0  # (0+100+100+200)/4 = 100
問題3: high_contrastの方が大きい
       - low_contrast: std = 0.0（全部同じ値）
       - high_contrast: std = 50.0（0と100に散らばっている）
```
</details>

---

# Day 2: OpenCV基礎（2〜3時間）

## 2.1 画像の読み込みと表示

### 学習目標
- [ ] `cv2.imread()`で画像を読み込める
- [ ] OpenCVがBGR形式で読み込むことを理解する

### 学ぶべき内容

#### 2.1.1 画像の読み込み
```python
import cv2

# 画像を読み込む
image = cv2.imread('sample.jpg')

# 読み込めたか確認
if image is None:
    print("画像が読み込めませんでした")
else:
    print(f"画像サイズ: {image.shape}")
```

**重要ポイント**:
- `cv2.imread()`はNumPy配列を返す
- 読み込みに失敗すると`None`を返す（エラーにならない！）
- カラー画像は**BGR形式**（青・緑・赤の順）で読み込まれる

#### 2.1.2 グレースケール読み込み
```python
# グレースケールで直接読み込む
gray = cv2.imread('sample.jpg', cv2.IMREAD_GRAYSCALE)
print(gray.shape)  # (高さ, 幅) ← 2次元になる
```

### 💡 プロジェクトでの使用場面
- Day 1: 画像を読み込んで`shape`と`dtype`を確認
- 全体を通じて: 画像ファイルの読み込みに使用

### 🔗 公式リファレンス
- [cv2.imread](https://docs.opencv.org/4.x/d4/da8/group__imgcodecs.html#ga288b8b3da0892bd651fce07b3bbd3a56)

---

## 2.2 色空間の変換

### 学習目標
- [ ] `cv2.cvtColor()`でカラー変換ができる
- [ ] BGR → グレースケール変換を理解する

### 学ぶべき内容

#### 2.2.1 グレースケール変換
```python
import cv2

# カラー画像を読み込み
color_image = cv2.imread('sample.jpg')

# グレースケールに変換
gray_image = cv2.cvtColor(color_image, cv2.COLOR_BGR2GRAY)

print(color_image.shape)  # (480, 640, 3) ← 3チャンネル
print(gray_image.shape)   # (480, 640)    ← 1チャンネル（2次元）
```

**なぜグレースケール変換が必要？**
- 輝度計算を簡単にするため
- 指標計算は基本的にグレースケールで行う
- 計算量が減る（1チャンネル vs 3チャンネル）

#### 2.2.2 BGR → RGB変換（matplotlib表示用）
```python
import cv2
import matplotlib.pyplot as plt

image = cv2.imread('sample.jpg')
image_rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

# matplotlibで表示
plt.imshow(image_rgb)
plt.show()
```

### 💡 プロジェクトでの使用場面
- Day 2: カラー/グレースケールの違い確認
- Day 3〜5: 指標計算の前処理

### 🔗 公式リファレンス
- [cv2.cvtColor](https://docs.opencv.org/4.x/d8/d01/group__imgproc__color__conversions.html#ga397ae87e1288a81d2363b61574eb8cab)

---

## 2.3 Laplacianフィルタ（シャープネス計算用）

### 学習目標
- [ ] Laplacianフィルタの概念を理解する
- [ ] `cv2.Laplacian()`の使い方を習得する

### 学ぶべき内容

#### 2.3.1 Laplacianフィルタとは
**ざっくり説明**:  
画像の「輪郭」や「エッジ」を検出するフィルタ。  
ピントが合っている画像 → エッジがくっきり → Laplacian値が大きい  
ピンぼけ画像 → エッジがぼんやり → Laplacian値が小さい

**技術的説明**:  
Laplacianフィルタは2次微分を計算する。輝度の急激な変化（エッジ）を検出する。

```python
import cv2
import numpy as np

# グレースケール画像を用意
gray = cv2.imread('sample.jpg', cv2.IMREAD_GRAYSCALE)

# Laplacianフィルタを適用
laplacian = cv2.Laplacian(gray, cv2.CV_64F)

# 分散を計算 → シャープネスの指標
sharpness = np.var(laplacian)
print(f"シャープネス: {sharpness}")
```

**パラメータ説明**:
- 第1引数: 入力画像（グレースケール）
- 第2引数: 出力のデータ型（`cv2.CV_64F`で64bit浮動小数点）

### 💡 プロジェクトでの使用場面
- Day 5: `calc_sharpness()` 関数の実装

### 🔗 公式リファレンス
- [cv2.Laplacian](https://docs.opencv.org/4.x/d4/d86/group__imgproc__filter.html#gad78703e4c8fe703d479c1860d76429e6)

---

## 2.4 Gaussianぼかし（人工ピンぼけ生成用）

### 学習目標
- [ ] `cv2.GaussianBlur()`でぼかし処理ができる
- [ ] σ（シグマ）の意味を理解する

### 学ぶべき内容

#### 2.4.1 Gaussianぼかしとは
**ざっくり説明**:  
画像を「ぼかす」処理。σ（シグマ）が大きいほど強くぼける。

```python
import cv2

# 画像読み込み
image = cv2.imread('sample.jpg')

# Gaussianぼかしを適用
# (5, 5): カーネルサイズ（奇数で指定）
# 0: σを自動計算（カーネルサイズから決定）
blurred = cv2.GaussianBlur(image, (5, 5), 0)

# σを明示的に指定する場合
blurred_sigma3 = cv2.GaussianBlur(image, (0, 0), sigmaX=3)
```

**パラメータ説明**:
- 第1引数: 入力画像
- 第2引数: カーネルサイズ（奇数×奇数）。`(0, 0)`でσから自動決定
- 第3引数: sigmaX（横方向のぼかし強度）

#### 2.4.2 σ（シグマ）の効果
```python
import cv2

image = cv2.imread('sample.jpg')

# σを変えてぼかしの強さを変える
blur_s1 = cv2.GaussianBlur(image, (0, 0), sigmaX=1)  # 弱いぼかし
blur_s3 = cv2.GaussianBlur(image, (0, 0), sigmaX=3)  # 中程度
blur_s5 = cv2.GaussianBlur(image, (0, 0), sigmaX=5)  # 強いぼかし
```

### 💡 プロジェクトでの使用場面
- Day 8〜9: 人工ピンぼけ画像の生成

### 🔗 公式リファレンス
- [cv2.GaussianBlur](https://docs.opencv.org/4.x/d4/d86/group__imgproc__filter.html#gaabe8c836e97159a9193fb0b11ac52cf1)

---

## 2.5 ✅ OpenCV確認問題

```python
import cv2
import numpy as np

# 問題1: 以下のコードでgray_imageのshapeは？
color = cv2.imread('sample.jpg')  # shape: (480, 640, 3)
gray_image = cv2.cvtColor(color, cv2.COLOR_BGR2GRAY)
# gray_image.shape → ?

# 問題2: シャープネス計算の正しい順序は？
# A: Laplacian → mean
# B: Laplacian → var
# C: GaussianBlur → var

# 問題3: ぼかしを強くするには？
# A: sigmaXを大きくする
# B: sigmaXを小さくする
```

<details>
<summary>解答を見る</summary>

```
問題1: (480, 640)
       グレースケール変換で3チャンネル→1チャンネルになる

問題2: B（Laplacian → var）
       Laplacianでエッジを検出し、その分散でシャープネスを測る

問題3: A（sigmaXを大きくする）
       σが大きいほどぼかしが強くなる
```
</details>

---

# Day 3: Python関数・エラー処理（1〜2時間）

## 3.1 関数の定義と戻り値

### 学習目標
- [ ] 引数と戻り値を持つ関数が書ける
- [ ] 型ヒント（Type Hints）の書き方を理解する

### 学ぶべき内容

#### 3.1.1 基本的な関数定義
```python
def calc_brightness(image):
    """画像の平均輝度を計算する"""
    return np.mean(image)
```

#### 3.1.2 型ヒント付きの関数（推奨）
```python
import numpy as np
from numpy.typing import NDArray

def calc_brightness(image: NDArray[np.uint8]) -> float:
    """
    画像の平均輝度を計算する
    
    Parameters
    ----------
    image : NDArray[np.uint8]
        入力画像（グレースケール）
    
    Returns
    -------
    float
        平均輝度値（0.0〜255.0）
    """
    return float(np.mean(image))
```

**型ヒントの意味**:
- `image: NDArray[np.uint8]` → 引数はNumPy配列（uint8型）
- `-> float` → 戻り値はfloat型

### 💡 プロジェクトでの使用場面
- Day 3〜5: 各指標計算関数の実装
- Day 12: 判定ロジックの関数化

---

## 3.2 例外処理（try-except）

### 学習目標
- [ ] 基本的なtry-except文が書ける
- [ ] 画像読み込みエラーを適切に処理できる

### 学ぶべき内容

#### 3.2.1 基本構文
```python
try:
    # エラーが起きる可能性のある処理
    result = risky_operation()
except Exception as e:
    # エラー時の処理
    print(f"エラーが発生しました: {e}")
```

#### 3.2.2 画像読み込みでの実践例
```python
import cv2

def load_image(filepath: str):
    """画像を安全に読み込む"""
    try:
        image = cv2.imread(filepath)
        if image is None:
            raise ValueError(f"画像を読み込めませんでした: {filepath}")
        return image
    except Exception as e:
        print(f"エラー: {e}")
        return None
```

**ポイント**:
- `cv2.imread()`は失敗しても例外を投げない（`None`を返す）
- 明示的に`ValueError`を発生させてエラーを検知可能にする

### 💡 プロジェクトでの使用場面
- Day 7: エラーハンドリング
- Day 20: UI安定化

### 🔗 公式リファレンス
- [Python例外処理](https://docs.python.org/ja/3/tutorial/errors.html)

---

## 3.3 パス操作（pathlib）

### 学習目標
- [ ] `pathlib.Path`でファイルパスを扱える
- [ ] 相対パス・絶対パスの違いを理解する

### 学ぶべき内容

#### 3.3.1 基本的な使い方
```python
from pathlib import Path

# パスの作成
data_dir = Path("data")
image_path = data_dir / "sample.jpg"

# パスの存在確認
if image_path.exists():
    print("ファイルが存在します")

# ファイル名・拡張子の取得
print(image_path.name)    # sample.jpg
print(image_path.stem)    # sample
print(image_path.suffix)  # .jpg
```

#### 3.3.2 プロジェクトルートの設定
```python
from pathlib import Path

# プロジェクトルートを基準にする
BASE_DIR = Path(__file__).parent.parent  # scripts/ から見て2階層上
DATA_DIR = BASE_DIR / "data"
```

**Notebookでの注意**:
```python
# Notebookでは __file__ が使えないので手動で設定
from pathlib import Path
BASE_DIR = Path("/path/to/project")  # プロジェクトのルートを指定
```

### 💡 プロジェクトでの使用場面
- Day 1: ディレクトリ構造の設定
- Day 9: データセット作成時のフォルダ整理

### 🔗 公式リファレンス
- [pathlib](https://docs.python.org/ja/3/library/pathlib.html)

---

## 3.4 ✅ Python確認問題

```python
# 問題1: 以下の関数の戻り値の型は？
def calc_contrast(image) -> float:
    return np.std(image)

# 問題2: 以下のコードでエラーが発生した場合、何が出力される？
try:
    image = cv2.imread("not_exist.jpg")
    if image is None:
        raise ValueError("読み込み失敗")
except ValueError as e:
    print(f"キャッチ: {e}")

# 問題3: Path("data") / "images" / "sample.jpg" の結果は？
```

<details>
<summary>解答を見る</summary>

```
問題1: float（型ヒントで明示されている）

問題2: 「キャッチ: 読み込み失敗」が出力される
       cv2.imread()はNoneを返すので、明示的にValueErrorを発生させている

問題3: Path("data/images/sample.jpg") 
       または "data/images/sample.jpg"（OSによって区切り文字が変わる）
```
</details>

---

# Day 4: Streamlit基礎（3〜4時間）

## 4.1 Streamlitとは

### 学習目標
- [ ] Streamlitの基本概念を理解する
- [ ] 簡単なアプリを起動できる

### 学ぶべき内容

#### 4.1.1 Streamlitの特徴
**ざっくり説明**:  
Pythonだけでウェブアプリが作れるライブラリ。  
HTML/CSS/JavaScriptの知識不要。

**技術的説明**:  
- スクリプトを上から順に実行してUIを生成
- ユーザー操作があるとスクリプト全体が再実行される
- 状態管理は`st.session_state`で行う

#### 4.1.2 最小限のアプリ
```python
# app.py
import streamlit as st

st.title("Hello Streamlit!")
st.write("これは最初のStreamlitアプリです")
```

**起動方法**:
```bash
streamlit run app.py
```

### 🔗 公式リファレンス
- [Streamlit公式ドキュメント](https://docs.streamlit.io/)
- [Get started](https://docs.streamlit.io/get-started)

---

## 4.2 ファイルアップロード

### 学習目標
- [ ] `st.file_uploader()`で画像をアップロードできる
- [ ] アップロードされたファイルをOpenCVで処理できる

### 学ぶべき内容

```python
import streamlit as st
import cv2
import numpy as np

st.title("画像アップローダー")

# ファイルアップロード
uploaded_file = st.file_uploader(
    "画像を選択してください",
    type=["jpg", "jpeg", "png"]
)

if uploaded_file is not None:
    # バイトデータをNumPy配列に変換
    file_bytes = np.asarray(bytearray(uploaded_file.read()), dtype=np.uint8)
    
    # OpenCVで画像としてデコード
    image = cv2.imdecode(file_bytes, cv2.IMREAD_COLOR)
    
    # 画像を表示
    st.image(image, channels="BGR", caption="アップロードされた画像")
    st.write(f"サイズ: {image.shape}")
```

**ポイント**:
- `uploaded_file`は最初`None`。ファイルがアップロードされると値が入る
- `st.image()`でOpenCV画像を表示するときは`channels="BGR"`を指定

### 💡 プロジェクトでの使用場面
- Day 15: 画像アップロード機能

### 🔗 公式リファレンス
- [st.file_uploader](https://docs.streamlit.io/develop/api-reference/widgets/st.file_uploader)

---

## 4.3 スライダー

### 学習目標
- [ ] `st.slider()`でしきい値を調整できる

### 学ぶべき内容

```python
import streamlit as st

st.title("しきい値調整")

# スライダー
brightness_threshold = st.slider(
    "明るさのしきい値",
    min_value=0,
    max_value=255,
    value=100,  # デフォルト値
    step=1
)

st.write(f"現在の値: {brightness_threshold}")

# 範囲スライダー
contrast_range = st.slider(
    "コントラストの範囲",
    min_value=0.0,
    max_value=100.0,
    value=(20.0, 80.0),  # タプルで範囲を指定
    step=0.5
)

st.write(f"下限: {contrast_range[0]}, 上限: {contrast_range[1]}")
```

### 💡 プロジェクトでの使用場面
- Day 17: しきい値UI化

### 🔗 公式リファレンス
- [st.slider](https://docs.streamlit.io/develop/api-reference/widgets/st.slider)

---

## 4.4 テーブル表示

### 学習目標
- [ ] `st.dataframe()`でデータを表示できる
- [ ] `st.table()`との違いを理解する

### 学ぶべき内容

```python
import streamlit as st
import pandas as pd

# サンプルデータ
data = {
    "指標": ["明るさ", "コントラスト", "シャープネス"],
    "値": [125.5, 45.2, 1250.0],
    "判定": ["OK", "OK", "NG"]
}
df = pd.DataFrame(data)

# インタラクティブなテーブル
st.subheader("st.dataframe（インタラクティブ）")
st.dataframe(df, use_container_width=True)

# 静的なテーブル
st.subheader("st.table（静的）")
st.table(df)
```

**違い**:
- `st.dataframe()`: ソートやスクロールが可能
- `st.table()`: 静的な表示のみ

### 💡 プロジェクトでの使用場面
- Day 16: 指標の表形式表示

### 🔗 公式リファレンス
- [st.dataframe](https://docs.streamlit.io/develop/api-reference/data/st.dataframe)

---

## 4.5 ダウンロードボタン

### 学習目標
- [ ] `st.download_button()`でCSVをダウンロードできる

### 学ぶべき内容

```python
import streamlit as st
import pandas as pd

# データ作成
df = pd.DataFrame({
    "ファイル名": ["image1.jpg", "image2.jpg"],
    "明るさ": [125.5, 98.3],
    "判定": ["OK", "NG"]
})

# CSVに変換
csv = df.to_csv(index=False)

# ダウンロードボタン
st.download_button(
    label="📥 CSVをダウンロード",
    data=csv,
    file_name="quality_check_result.csv",
    mime="text/csv"
)
```

### 💡 プロジェクトでの使用場面
- Day 19: CSV出力機能

### 🔗 公式リファレンス
- [st.download_button](https://docs.streamlit.io/develop/api-reference/widgets/st.download_button)

---

## 4.6 ✅ Streamlit確認問題

```python
# 問題1: 以下のコードでファイルがアップロードされていない場合、
#        if文の中は実行される？
uploaded_file = st.file_uploader("ファイルを選択")
if uploaded_file:
    st.write("ファイルがあります")

# 問題2: st.slider()で範囲を選択するには？
# A: value=(10, 90) とタプルで指定
# B: range=True と指定
# C: 範囲選択はできない

# 問題3: st.dataframe()とst.table()の違いは？
```

<details>
<summary>解答を見る</summary>

```
問題1: 実行されない
       uploaded_fileはNoneなのでFalsyとして扱われる

問題2: A（value=(10, 90) とタプルで指定）

問題3: 
- st.dataframe(): インタラクティブ（ソート・スクロール可能）
- st.table(): 静的表示のみ
```
</details>

---

# Day 5: pandas基礎 + 総復習（1〜2時間）

## 5.1 DataFrame基礎

### 学習目標
- [ ] 辞書からDataFrameを作成できる
- [ ] CSVファイルに出力できる

### 学ぶべき内容

#### 5.1.1 辞書からDataFrame作成
```python
import pandas as pd

# 辞書のリストから作成
results = [
    {"filename": "image1.jpg", "brightness": 125.5, "status": "OK"},
    {"filename": "image2.jpg", "brightness": 98.3, "status": "NG"},
]
df = pd.DataFrame(results)
print(df)
```

出力:
```
      filename  brightness status
0  image1.jpg       125.5     OK
1  image2.jpg        98.3     NG
```

#### 5.1.2 CSV出力
```python
# CSVファイルに保存
df.to_csv("result.csv", index=False)

# 文字列として取得（Streamlitダウンロード用）
csv_string = df.to_csv(index=False)
```

### 💡 プロジェクトでの使用場面
- Day 13: 結果のdict化
- Day 19: CSVダウンロード

### 🔗 公式リファレンス
- [pandas.DataFrame](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.html)
- [DataFrame.to_csv](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.to_csv.html)

---

## 5.2 matplotlib検証用（最小限）

### 学習目標
- [ ] 画像を表示できる
- [ ] 簡単なグラフを作成できる

### 学ぶべき内容

```python
import matplotlib.pyplot as plt
import cv2

# 画像表示（Notebook検証用）
image = cv2.imread("sample.jpg")
image_rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

plt.figure(figsize=(8, 6))
plt.imshow(image_rgb)
plt.title("Sample Image")
plt.axis("off")
plt.show()

# 棒グラフ（指標比較用）
metrics = ["Brightness", "Contrast", "Sharpness"]
values = [125.5, 45.2, 1250.0]

plt.figure(figsize=(8, 4))
plt.bar(metrics, values)
plt.title("Quality Metrics")
plt.show()
```

### 💡 プロジェクトでの使用場面
- Day 2: 画像表示確認
- Day 10: 指標挙動の可視化

### 🔗 公式リファレンス
- [matplotlib.pyplot](https://matplotlib.org/stable/api/pyplot_summary.html)

---

## 5.3 総復習チェックリスト

### NumPy
- [ ] `shape`, `dtype`で配列の情報を確認できる
- [ ] `np.mean()`で平均を計算できる
- [ ] `np.std()`で標準偏差を計算できる
- [ ] `np.var()`で分散を計算できる

### OpenCV
- [ ] `cv2.imread()`で画像を読み込める
- [ ] `cv2.cvtColor()`でグレースケール変換できる
- [ ] `cv2.Laplacian()`でエッジ検出できる
- [ ] `cv2.GaussianBlur()`でぼかし処理できる

### Python
- [ ] 型ヒント付きの関数を定義できる
- [ ] try-exceptでエラーを処理できる
- [ ] pathlibでパスを扱える

### Streamlit
- [ ] `st.file_uploader()`でファイルをアップロードできる
- [ ] `st.slider()`でしきい値を調整できる
- [ ] `st.dataframe()`でテーブル表示できる
- [ ] `st.download_button()`でCSVをダウンロードできる

### pandas
- [ ] 辞書からDataFrameを作成できる
- [ ] `to_csv()`でCSV出力できる

---

## 📝 学習完了後の次のステップ

このアジェンダを完了したら、プロジェクト計画書の**Week 1 Day 1**から開発を開始してください。

学習中に不明点があれば、いつでも質問してください！
