# Day 5: pandas基礎 + 総復習（1〜2時間）

---

## 📋 今日のゴール

表形式のデータを扱う「pandas」の基本を学び、これまでの学習内容を総復習します。  
Day 1〜4で学んだことを整理して、プロジェクト開発に必要な知識を確認します。

---

## 🎯 学習目標

| # | 目標 | 確認 |
|---|------|------|
| 1 | 辞書からDataFrameを作成できる | ☐ |
| 2 | DataFrameをCSVファイルに出力できる | ☐ |
| 3 | matplotlibで画像やグラフを表示できる | ☐ |
| 4 | Day 1〜4の内容を総復習して理解を確認する | ☐ |

---

## 📚 用語表

| 用語 | 読み方 | 意味（かんたんに） | 意味（くわしく） |
|------|--------|-------------------|-----------------|
| pandas | パンダス | 表形式のデータを扱うライブラリ | Pythonでデータ分析を行うための主要ライブラリ。ExcelやCSVのような表形式データを効率的に処理できる |
| DataFrame | データフレーム | 表（行と列があるデータ） | pandasの主要なデータ構造。2次元のラベル付きデータで、各列は異なるデータ型を持てる |
| Series | シリーズ | 1列分のデータ | pandasの1次元のラベル付きデータ構造。DataFrameの1列を取り出すとSeriesになる |
| CSV | シーエスブイ | カンマ区切りのテキストファイル | Comma-Separated Values。データをカンマで区切って保存するシンプルな形式 |
| index | インデックス | 行番号・行名 | DataFrameの各行を識別するためのラベル。デフォルトは0, 1, 2... |
| matplotlib | マットプロットリブ | グラフを描くライブラリ | Pythonで最も広く使われているグラフ描画ライブラリ |

---

## 5.1 DataFrame基礎

### ざっくり説明（まずはここから！）

**pandas**は、Excelのような**表形式のデータ**をPythonで扱うためのライブラリです。

```
【Excelでのイメージ】
┌───┬────────────┬────────┬──────┐
│   │ ファイル名 │ 明るさ │ 判定 │
├───┼────────────┼────────┼──────┤
│ 0 │ image1.jpg │ 125.5  │ OK   │
│ 1 │ image2.jpg │ 98.3   │ NG   │
└───┴────────────┴────────┴──────┘

【pandasでのイメージ】
→ これがDataFrame！
```

DataFrameを使うと：
- データを表形式で整理できる
- CSVファイルに保存できる
- 計算や集計が簡単にできる

### くわしい説明

#### 5.1.1 辞書からDataFrameを作成する

DataFrameを作る方法はいくつかありますが、プロジェクトでよく使う方法を2つ紹介します。

**方法1: 辞書から作成（列ベース）**

```python
import pandas as pd

# 辞書のキーが列名、値がその列のデータ
data = {
    "filename": ["image1.jpg", "image2.jpg", "image3.jpg"],
    "brightness": [125.5, 98.3, 150.2],
    "status": ["OK", "NG", "OK"]
}

df = pd.DataFrame(data)
print(df)
```

出力：
```
      filename  brightness status
0  image1.jpg       125.5     OK
1  image2.jpg        98.3     NG
2  image3.jpg       150.2     OK
```

**方法2: 辞書のリストから作成（行ベース）← プロジェクトでよく使う！**

```python
import pandas as pd

# 1行分のデータを辞書で表し、それをリストに追加していく
results = [
    {"filename": "image1.jpg", "brightness": 125.5, "status": "OK"},
    {"filename": "image2.jpg", "brightness": 98.3, "status": "NG"},
    {"filename": "image3.jpg", "brightness": 150.2, "status": "OK"},
]

df = pd.DataFrame(results)
print(df)
```

出力は同じ：
```
      filename  brightness status
0  image1.jpg       125.5     OK
1  image2.jpg        98.3     NG
2  image3.jpg       150.2     OK
```

#### なぜ「辞書のリスト」がプロジェクトで便利なのか

画像を1枚ずつ処理して、結果を追加していく場合に自然に書けるからです：

```python
import pandas as pd

# 空のリストを用意
results = []

# 画像ファイルのリスト（イメージ）
image_files = ["image1.jpg", "image2.jpg", "image3.jpg"]

# 1枚ずつ処理
for filename in image_files:
    # 画像を処理して指標を計算（ここは仮の値）
    brightness = 100.0  # 実際は画像から計算
    
    # 1行分の結果を辞書として追加
    results.append({
        "filename": filename,
        "brightness": brightness,
        "status": "OK" if brightness >= 100 else "NG"
    })

# 最後にDataFrameに変換
df = pd.DataFrame(results)
print(df)
```

#### 5.1.2 DataFrameをCSVに出力する

```python
import pandas as pd

# DataFrameを作成
df = pd.DataFrame([
    {"filename": "image1.jpg", "brightness": 125.5, "status": "OK"},
    {"filename": "image2.jpg", "brightness": 98.3, "status": "NG"},
])

# ===== ファイルに保存する場合 =====
df.to_csv("result.csv", index=False)
# → result.csv というファイルが作成される

# ===== 文字列として取得する場合（Streamlitダウンロード用） =====
csv_string = df.to_csv(index=False)
print(csv_string)
```

出力（`csv_string`の中身）：
```
filename,brightness,status
image1.jpg,125.5,OK
image2.jpg,98.3,NG
```

#### `index=False`の意味

```python
# index=True（デフォルト）の場合
print(df.to_csv())
# 出力:
# ,filename,brightness,status
# 0,image1.jpg,125.5,OK
# 1,image2.jpg,98.3,NG
#
# ↑ 行番号（0, 1）も出力される

# index=False の場合
print(df.to_csv(index=False))
# 出力:
# filename,brightness,status
# image1.jpg,125.5,OK
# image2.jpg,98.3,NG
#
# ↑ 行番号は出力されない（すっきり！）
```

> 💡 **ポイント**: 通常は`index=False`を指定することが多いです。行番号（インデックス）は分析用の内部的な番号であり、出力結果には不要なことが多いためです。

### 💡 プロジェクトでの使用場面

- **Day 13**: 処理結果を辞書として整理
- **Day 19**: CSVダウンロード機能の実装

### 🔍 ここがわからなければ確認しよう

- **辞書`{}`の書き方がわからない**: Pythonの辞書型の基礎を確認
- **リスト`[]`と`append()`がわからない**: Pythonのリスト型の基礎を確認
- **forループがわからない**: Pythonの繰り返し処理の基礎を確認

### 🔗 公式リファレンス

- [pandas.DataFrame](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.html)
- [DataFrame.to_csv](https://pandas.pydata.org/docs/reference/api/pandas.DataFrame.to_csv.html)

---

## 5.2 matplotlib検証用（最小限）

### ざっくり説明

**matplotlib**（マットプロットリブ）は、Pythonでグラフや画像を表示するためのライブラリです。

Jupyter Notebookで検証するときに、画像を表示したり、指標を棒グラフで比較したりするのに使います。

> 💡 **補足**: Streamlitには`st.image()`があるので、アプリ内での画像表示にはmatplotlibは使いません。主にNotebookでの検証用です。

### くわしい説明

#### 5.2.1 画像を表示する

```python
import matplotlib.pyplot as plt
import cv2

# 画像を読み込む
image = cv2.imread("sample.jpg")

# OpenCVはBGR形式なので、RGBに変換
image_rgb = cv2.cvtColor(image, cv2.COLOR_BGR2RGB)

# 画像を表示
plt.figure(figsize=(8, 6))  # 図のサイズ（幅8インチ、高さ6インチ）
plt.imshow(image_rgb)       # 画像を表示
plt.title("Sample Image")   # タイトル
plt.axis("off")             # 軸を非表示
plt.show()                  # 表示を実行
```

#### コードの流れ

```
cv2.imread()       画像を読み込む（BGR形式）
        ↓
cv2.cvtColor()     RGBに変換（matplotlibはRGBを期待）
        ↓
plt.figure()       図の準備（サイズ指定）
        ↓
plt.imshow()       画像をセット
        ↓
plt.show()         実際に表示
```

#### 5.2.2 棒グラフを作成する

指標の比較や、しきい値との関係を可視化するのに便利です。

```python
import matplotlib.pyplot as plt

# データ
metrics = ["Brightness", "Contrast", "Sharpness"]
values = [125.5, 45.2, 1250.0]

# 棒グラフを作成
plt.figure(figsize=(8, 4))
plt.bar(metrics, values)      # 棒グラフ
plt.title("Quality Metrics")  # タイトル
plt.ylabel("Value")           # Y軸のラベル
plt.show()
```

#### 5.2.3 複数の画像を並べて表示する

元画像とぼかした画像を比較するときなどに便利です。

```python
import matplotlib.pyplot as plt
import cv2

# 画像を読み込み
original = cv2.imread("sample.jpg")
original_rgb = cv2.cvtColor(original, cv2.COLOR_BGR2RGB)

# ぼかし画像を作成
blurred = cv2.GaussianBlur(original, (0, 0), sigmaX=5)
blurred_rgb = cv2.cvtColor(blurred, cv2.COLOR_BGR2RGB)

# 横に2枚並べて表示
fig, axes = plt.subplots(1, 2, figsize=(12, 5))
# ↑ 1行2列のレイアウト、図のサイズは幅12×高さ5インチ

# 左側：元画像
axes[0].imshow(original_rgb)
axes[0].set_title("Original")
axes[0].axis("off")

# 右側：ぼかし画像
axes[1].imshow(blurred_rgb)
axes[1].set_title("Blurred (sigma=5)")
axes[1].axis("off")

plt.tight_layout()  # レイアウトを自動調整
plt.show()
```

#### `plt.subplots()`の仕組み

```python
# 1行2列のレイアウトを作成
fig, axes = plt.subplots(1, 2)

# fig: 図全体
# axes: 個々のグラフ領域（この場合は2つ）

axes[0]  # 左側のグラフ領域
axes[1]  # 右側のグラフ領域
```

### 💡 プロジェクトでの使用場面

- **Day 2**: 画像表示の確認（Notebookで）
- **Day 10**: 指標の挙動を可視化して確認

### 🔍 ここがわからなければ確認しよう

- **`fig, axes = ...`の書き方がわからない**: Pythonのアンパック代入を確認
- **`axes[0]`がわからない**: Pythonのリストとインデックスを確認
- **BGR/RGBがわからない**: Day 2のOpenCV基礎を復習

### 🔗 公式リファレンス

- [matplotlib.pyplot](https://matplotlib.org/stable/api/pyplot_summary.html)
- [plt.subplots](https://matplotlib.org/stable/api/_as_gen/matplotlib.pyplot.subplots.html)

---

## 5.3 総復習チェックリスト

Day 1〜5で学んだ内容を確認しましょう。各項目ができるかどうかチェックしてください。

### ✅ NumPy（Day 1）

| 項目 | 確認 | できなかったら |
|------|------|---------------|
| `shape`で配列のサイズを確認できる | ☐ | Day 1 セクション1.1を復習 |
| `dtype`でデータ型を確認できる | ☐ | Day 1 セクション1.1を復習 |
| `np.mean()`で平均を計算できる | ☐ | Day 1 セクション1.2を復習 |
| `np.std()`で標準偏差を計算できる | ☐ | Day 1 セクション1.2を復習 |
| `np.var()`で分散を計算できる | ☐ | Day 1 セクション1.2を復習 |

**復習用コード**:
```python
import numpy as np

# 2次元配列を作成
arr = np.array([[100, 150], [200, 250]])

print(f"shape: {arr.shape}")     # (2, 2)
print(f"dtype: {arr.dtype}")     # int64
print(f"mean: {np.mean(arr)}")   # 175.0
print(f"std: {np.std(arr)}")     # 55.9...
print(f"var: {np.var(arr)}")     # 3125.0
```

### ✅ OpenCV（Day 2）

| 項目 | 確認 | できなかったら |
|------|------|---------------|
| `cv2.imread()`で画像を読み込める | ☐ | Day 2 セクション2.1を復習 |
| 読み込み失敗時の`None`チェックができる | ☐ | Day 2 セクション2.1を復習 |
| `cv2.cvtColor()`でグレースケール変換できる | ☐ | Day 2 セクション2.2を復習 |
| `cv2.Laplacian()`でエッジ検出できる | ☐ | Day 2 セクション2.3を復習 |
| `cv2.GaussianBlur()`でぼかし処理できる | ☐ | Day 2 セクション2.4を復習 |

**復習用コード**:
```python
import cv2
import numpy as np

# 画像読み込み
image = cv2.imread("sample.jpg")

# Noneチェック
if image is None:
    print("読み込み失敗")
else:
    print(f"画像サイズ: {image.shape}")

# グレースケール変換
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

# Laplacianフィルタ（シャープネス計算用）
laplacian = cv2.Laplacian(gray, cv2.CV_64F)
sharpness = np.var(laplacian)

# Gaussianぼかし
blurred = cv2.GaussianBlur(image, (0, 0), sigmaX=3)
```

### ✅ Python関数・エラー処理（Day 3）

| 項目 | 確認 | できなかったら |
|------|------|---------------|
| 引数と戻り値を持つ関数を定義できる | ☐ | Day 3 セクション3.1を復習 |
| 型ヒントを書ける | ☐ | Day 3 セクション3.1を復習 |
| try-exceptでエラー処理ができる | ☐ | Day 3 セクション3.2を復習 |
| pathlibでパスを扱える | ☐ | Day 3 セクション3.3を復習 |

**復習用コード**:
```python
from pathlib import Path
import numpy as np
from numpy.typing import NDArray

# 型ヒント付き関数
def calc_brightness(image: NDArray[np.uint8]) -> float:
    """画像の平均輝度を計算する"""
    return float(np.mean(image))

# エラー処理
def load_image_safe(filepath: str):
    try:
        image = cv2.imread(filepath)
        if image is None:
            raise ValueError(f"読み込み失敗: {filepath}")
        return image
    except Exception as e:
        print(f"エラー: {e}")
        return None

# pathlib
data_dir = Path("data")
image_path = data_dir / "sample.jpg"
print(image_path.exists())  # ファイルが存在するか
```

### ✅ Streamlit（Day 4）

| 項目 | 確認 | できなかったら |
|------|------|---------------|
| `streamlit run app.py`で起動できる | ☐ | Day 4 セクション4.1を復習 |
| `st.file_uploader()`でファイルをアップロードできる | ☐ | Day 4 セクション4.2を復習 |
| `st.slider()`で数値を調整できる | ☐ | Day 4 セクション4.3を復習 |
| `st.dataframe()`でテーブル表示できる | ☐ | Day 4 セクション4.4を復習 |
| `st.download_button()`でCSVダウンロードできる | ☐ | Day 4 セクション4.5を復習 |

**復習用コード**:
```python
import streamlit as st
import pandas as pd

# ファイルアップロード
uploaded = st.file_uploader("ファイル選択", type=["jpg", "png"])

# スライダー
threshold = st.slider("しきい値", 0, 255, 100)

# 範囲スライダー
range_val = st.slider("範囲", 0.0, 100.0, (20.0, 80.0))

# テーブル表示
df = pd.DataFrame([{"name": "test", "value": 100}])
st.dataframe(df)

# ダウンロード
csv = df.to_csv(index=False)
st.download_button("Download", csv, "result.csv", "text/csv")
```

### ✅ pandas（Day 5）

| 項目 | 確認 | できなかったら |
|------|------|---------------|
| 辞書からDataFrameを作成できる | ☐ | Day 5 セクション5.1を復習 |
| 辞書のリストからDataFrameを作成できる | ☐ | Day 5 セクション5.1を復習 |
| `to_csv()`でCSV出力できる | ☐ | Day 5 セクション5.1を復習 |

**復習用コード**:
```python
import pandas as pd

# 辞書のリストからDataFrame
results = [
    {"filename": "img1.jpg", "brightness": 125.5},
    {"filename": "img2.jpg", "brightness": 98.3},
]
df = pd.DataFrame(results)

# CSV文字列として取得
csv_string = df.to_csv(index=False)

# ファイルに保存
df.to_csv("output.csv", index=False)
```

---

## ✅ Day 5 確認問題

### 問題1: DataFrame作成

以下のコードの出力結果を予想してください。

```python
import pandas as pd

data = [
    {"name": "Alice", "score": 85},
    {"name": "Bob", "score": 72},
]
df = pd.DataFrame(data)
print(df.shape)
```

### 問題2: CSV出力

`df.to_csv(index=False)`と`df.to_csv()`の違いは何？

### 問題3: 総合問題

以下のコードで、シャープネスを計算する正しい順序はどれ？

```python
import cv2
import numpy as np

image = cv2.imread("sample.jpg")
# ここでシャープネスを計算したい
```

- A: `np.var(image)`
- B: `np.var(cv2.Laplacian(image, cv2.CV_64F))`
- C: `np.var(cv2.Laplacian(cv2.cvtColor(image, cv2.COLOR_BGR2GRAY), cv2.CV_64F))`

<details>
<summary>解答を見る</summary>

### 問題1の解答

**`(2, 3)`**

```python
print(df.shape)  # (2, 3)
# 2行（AliceとBob）
# 3列（index, name, score）... ではなく
# 2列（name, score）
# 
# 待って、正解は (2, 2) です！
# 2行 × 2列（nameとscore）
```

訂正：正解は **`(2, 2)`** です。
- 2行（AliceとBobの2レコード）
- 2列（nameとscoreの2カラム）

---

### 問題2の解答

| `df.to_csv()` | `df.to_csv(index=False)` |
|---------------|--------------------------|
| 行番号（0, 1, 2...）も出力される | 行番号は出力されない |

```
# df.to_csv() の出力
,name,score
0,Alice,85
1,Bob,72

# df.to_csv(index=False) の出力
name,score
Alice,85
Bob,72
```

---

### 問題3の解答

**C: `np.var(cv2.Laplacian(cv2.cvtColor(image, cv2.COLOR_BGR2GRAY), cv2.CV_64F))`**

シャープネス計算の正しい手順：
1. カラー画像をグレースケールに変換（`cv2.cvtColor()`）
2. Laplacianフィルタを適用（`cv2.Laplacian()`）
3. 分散を計算（`np.var()`）

```python
# 分解して書くと
gray = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)  # 1. グレースケール化
laplacian = cv2.Laplacian(gray, cv2.CV_64F)      # 2. Laplacian適用
sharpness = np.var(laplacian)                    # 3. 分散を計算
```

- A は画像全体の分散（シャープネスではない）
- B はカラー画像にLaplacianを適用している（グレースケール化が抜けている）

</details>

---

## 📊 知識マップ：プロジェクトでの使用タイミング

学んだ内容がプロジェクトのどこで使われるかをまとめます。

```
【事前学習】                      【プロジェクト開発】
                                  
Day 1: NumPy基礎 ─────────────→ Day 3〜5: 指標計算関数
  ├ shape, dtype                    ├ calc_brightness()
  ├ np.mean()                       ├ calc_contrast()
  ├ np.std()                        └ calc_sharpness()
  └ np.var()
                                  
Day 2: OpenCV基礎 ────────────→ Day 1〜5: 画像処理全般
  ├ cv2.imread()                    ├ 画像読み込み
  ├ cv2.cvtColor()                  ├ グレースケール変換
  ├ cv2.Laplacian()                 ├ シャープネス計算
  └ cv2.GaussianBlur()              └ Day 8〜9: 人工ピンぼけ生成
                                  
Day 3: 関数・エラー処理 ──────→ Day 7: エラーハンドリング
  ├ 型ヒント                         Day 12: 判定ロジック
  ├ try-except                      Day 20: UI安定化
  └ pathlib
                                  
Day 4: Streamlit ─────────────→ Day 15〜17: UI開発
  ├ file_uploader()                 ├ 画像アップロード
  ├ slider()                        ├ しきい値調整
  ├ dataframe()                     └ 結果表示
  └ download_button()             Day 19: CSV出力
                                  
Day 5: pandas ────────────────→ Day 13: 結果のdict化
  ├ DataFrame作成                   Day 19: CSVダウンロード
  └ to_csv()
```

---

## 📝 学習完了後の次のステップ

### 🎉 おめでとうございます！

Day 1〜5の事前学習を完了しました。これで、プロジェクト開発に必要な基礎知識が身につきました。

### 次にやること

1. **プロジェクト計画書の Week 1 Day 1** から開発を開始
2. 不明点があれば、該当するDayの資料に戻って復習
3. 困ったらいつでも質問してください！

### 学習のコツ

- **完璧を目指さない**: 70〜80%理解できていればOK。実際にコードを書きながら身につけていきましょう
- **エラーは友達**: エラーメッセージは問題解決のヒント。怖がらずに読んでみましょう
- **公式ドキュメントを活用**: 困ったときは公式ドキュメントが最も信頼できる情報源です

---

## 📚 参考リンクまとめ

### NumPy
- [NumPy公式ドキュメント](https://numpy.org/doc/stable/)
- [配列の属性](https://numpy.org/doc/stable/reference/arrays.ndarray.html#array-attributes)

### OpenCV
- [OpenCV公式ドキュメント](https://docs.opencv.org/4.x/)
- [画像処理チュートリアル](https://docs.opencv.org/4.x/d2/d96/tutorial_py_table_of_contents_imgproc.html)

### Python
- [Python公式ドキュメント（日本語）](https://docs.python.org/ja/3/)
- [pathlib](https://docs.python.org/ja/3/library/pathlib.html)
- [例外処理](https://docs.python.org/ja/3/tutorial/errors.html)

### Streamlit
- [Streamlit公式ドキュメント](https://docs.streamlit.io/)
- [APIリファレンス](https://docs.streamlit.io/develop/api-reference)

### pandas
- [pandas公式ドキュメント](https://pandas.pydata.org/docs/)
- [10 minutes to pandas](https://pandas.pydata.org/docs/user_guide/10min.html)

### matplotlib
- [matplotlib公式ドキュメント](https://matplotlib.org/stable/)
- [pyplot チュートリアル](https://matplotlib.org/stable/tutorials/pyplot.html)

---

## 🎉 お疲れさまでした！

5日間の事前学習、本当にお疲れさまでした。

これで、画像の読み込み・処理・指標計算・表示・出力という、プロジェクトの基本的な流れを実装するための知識が揃いました。

プロジェクト開発では、これらの知識を組み合わせて、実際に動くアプリケーションを作っていきます。最初は難しく感じることもあるかもしれませんが、一つずつ進めていけば必ず完成できます。

一緒に頑張りましょう！
